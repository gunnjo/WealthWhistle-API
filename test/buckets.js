var buckets = require('../models/buckets.js')
var mocha = require('mocha');

describe('Buckets', function() {
	describe('Create Buckets', function() {

		it('Should create new buckets', function() {

		});
	});
});